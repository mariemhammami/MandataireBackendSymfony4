<form method="post" action="{{ path('mandataires_test_delete', {'id': mandataires_test.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mandataires_test.id) }}">
    <button class="btn">Delete</button>
</form>
